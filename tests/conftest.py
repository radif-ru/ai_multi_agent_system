"""Глобальные фикстуры для тестов.

На момент Спринта 00 (Bootstrap) кода в `app/` ещё нет — заготовка фикстур пуста.
Содержательные фикстуры (`base_env`, `tmp_memory_db`, mock-фабрики `Message`/`Bot`)
наполняются по мере добавления соответствующих тестов в Спринте 01.

См. `_docs/testing.md` § «Моки внешних систем».
"""

from __future__ import annotations
